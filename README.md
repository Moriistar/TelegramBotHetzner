
🚀 TelegramBotHetzner

مدیریت کامل سرورهای Hetzner Cloud فقط با یک ربات تلگرام!

این ربات به شما اجازه می‌دهد بدون ورود به پنل Hetzner و فقط از طریق تلگرام:

ساخت VPS

حذف VPS

ریبوت

رینستال (Rebuild)

لیست سرورها

مشاهده وضعیت

مدیریت کامل از طریق API هرتزنر


و فقط توسط ادمین (آیدی عددی) قابل استفاده است.


---

✨ امکانات ربات

قابلیت	توضیح

🟢 ساخت سرور	ساخت سرور جدید در Hetzner
🗑 حذف سرور	حذف کامل VPS
🔄 ریبوت	ریستارت سرور
♻ رینستال	نصب دوباره سیستم‌عامل
📋 لیست سرورها	نمایش تمام سرورها
🔐 فقط برای ادمین	محدودسازی دسترسی
⚙ اتصال کامل به Hetzner API	بدون نیاز به پنل



---

📦 نصب سریع (One-Line Install)

پس از آپلود فایل‌ها در گیت‌هابت، با دستور زیر نصب کنید:

bash <(curl -s https://raw.githubusercontent.com/moriistar/TelegramBotHetzner/main/install.sh)

یا:

wget -O install.sh https://raw.githubusercontent.com/moriistar/TelegramBotHetzner/main/install.sh && bash install.sh


---

⚙️ مراحل نصب

در زمان اجرای install.sh از شما پرسیده می‌شود:

1. آیدی عددی ادمین


2. توکن ربات تلگرام


3. Hetzner API Token



اسکریپت نصب سپس:

فایل config.json را می‌سازد

پکیج‌های لازم را نصب می‌کند

سرویس systemd می‌سازد

ربات را اجرا می‌کند



---

📁 ساختار پروژه

TelegramBotHetzner/
│
├── bot.py
├── hetzner_api.py
├── config.json
├── requirements.txt
└── install.sh


---

🐍 اجرای دستی ربات

python3 bot.py


---

🔧 توسعه و شخصی‌سازی

bot.py → منطق ربات

hetzner_api.py → مدیریت API هرتزنر

install.sh → نصب خودکار

config.json → تنظیمات


